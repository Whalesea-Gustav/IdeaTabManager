<idea-plugin>
  <id>com.whalesea.ideatabmanager</id>
  <name>Tab Groups</name>
  <vendor email="whalesea.gustav@gmail.com" url="https://github.com/Whalesea-Gustav/IdeaTabManager">Whalesea-Gustav</vendor>

  <description><![CDATA[
    <p>Save open editor files as named, colored, persistent work groups and restore a coding context when you return to it.</p>
    <p>Manage groups from a compact Tool Window, batch-add files from the Project View, and optionally open a matching TortoiseSVN or TortoiseGit commit dialog for a group.</p>
  ]]></description>

  <change-notes><![CDATA[
    <h3>0.3.0 - Simplified Chinese localization</h3>
    <ul>
      <li>Added Simplified Chinese UI text for Tab Groups menus, dialogs, notifications, Tool Window controls, and color labels.</li>
      <li>Tab Groups now follows the JetBrains IDE UI language automatically: <code>zh_CN</code> uses Chinese and other locales use English.</li>
    </ul>
    <h3>0.2.11 - Faster Group file adding</h3>
    <ul>
      <li>Added Header buttons to add the active editor file or choose multiple open files for a Group.</li>
      <li>Existing Group files are excluded from the folder-organized picker and cannot be added twice.</li>
    </ul>
    <h3>0.2.10 — On-demand member controls</h3>
    <ul>
      <li>Added a per-Group adjustment switch after the note to show or hide file reorder and remove controls.</li>
      <li>Hidden the empty <code>Add note</code> placeholder while preserving note editing.</li>
      <li>Kept file-row controls adjacent to each file name without forced right-edge alignment.</li>
    </ul>
    <h3>0.2.9 — Safer group workflows and member organization</h3>
    <ul>
      <li>Added Undo for Group content and metadata changes, Group opening, and external-tab closing actions; reorder operations remain excluded.</li>
      <li>Added collapsible folder-based selection for saving, adding, and reviewing open tabs, plus persistent ordering and remove controls for Group members.</li>
      <li>Added <b>Open Group Tabs and Close Others…</b> with explicit review of tabs before closing.</li>
      <li>Kept member action buttons adjacent to each file name instead of forcing them into a shared right-aligned column.</li>
      <li>Removed internal and deprecated IntelliJ Platform API usages reported by Marketplace compatibility checks.</li>
    </ul>
    <h3>0.2.8 — Fixed Header color marker sizing</h3>
    <ul>
      <li>Fixed the Group Header color marker expanding across the row and crowding the title, <code>num={count}</code> file count, and note.</li>
    </ul>
    <h3>0.2.7 — Refined Header file count</h3>
    <ul>
      <li>Group file counts now use a compact, muted <code>num={count}</code> label, keeping the editable group title visually primary.</li>
    </ul>
    <h3>0.2.6 — Reliable CI validation</h3>
    <ul>
      <li>Aligned the JUnit Platform test runtime with Rider 2026.2 so GitHub Actions can initialize the IDE test listener reliably.</li>
    </ul>
    <h3>0.2.5 — Compact Header metadata</h3>
    <ul>
      <li>Fixed Header layout so the title no longer expands across the whole Tool Window and pushes the note away.</li>
      <li>Group titles and notes now remain adjacent with a compact, stable gap at every Tool Window width.</li>
    </ul>
    <h3>0.2.4 — Clearer multi-tab identity</h3>
    <ul>
      <li>Redesigned the Tool Window and plugin icons as a three-tab editor surface with one shared group rail.</li>
      <li>The icon now communicates managed tab groups at compact Tool Window and Plugin Manager sizes, with dedicated light and dark variants.</li>
    </ul>
    <h3>0.2.3 — Reliable group reordering</h3>
    <ul>
      <li>Fixed Group Header drag reordering in embedded Tool Windows by tracking the pointer directly inside the Tab Groups panel.</li>
      <li>The blue insertion line and the saved drop position now use the same target Group coordinates, including moves to the end of the list.</li>
    </ul>
    <h3>0.2.2 — Group ordering and quick add</h3>
    <ul>
      <li>Use <b>Add Open Tabs…</b> on a group Header to select currently open files and add them directly to that group.</li>
      <li>Drag the new six-dot Header grip to reorder Tab Groups. The insertion line shows whether the group will be placed before or after the target.</li>
      <li>Refined compact Header spacing between the title and note.</li>
    </ul>
    <h3>0.2.1 — Compact tab selection</h3>
    <ul>
      <li>Replaced the persistent Open Tabs checklist with <b>Save Selected Tabs</b>, keeping the Tool Window focused on groups.</li>
      <li>Select open files in a dedicated dialog with active-tab preselection plus <b>Select All</b> and <b>Clear</b> controls.</li>
      <li>Create a new group or add the selected files to an existing group directly from the selection dialog.</li>
      <li>Placed group titles and notes side by side in the Header for more compact scanning.</li>
    </ul>
    <h3>0.2.0 — Tab Group workflows and Tortoise commits</h3>
    <ul>
      <li>Manage named, colored, persistent Tab Groups from the <b>Tab Groups</b> Tool Window.</li>
      <li>Create groups from all open tabs or selected open tabs, add files from the Project View, and add files directly to a group.</li>
      <li>Collapse groups, add an optional note, and press <b>F2</b> on a selected header title or note to edit it inline.</li>
      <li>Choose other tabs to close one by one, or use the explicitly unsafe action to close every other tab without unsaved changes.</li>
      <li>Open <b>TortoiseSVN</b> or <b>TortoiseGit</b> Commit from a group header when its files belong to a detected local working copy.</li>
      <li>Mixed groups keep separate commit entries for distinct Git/SVN working-copy roots, so unrelated repositories are never combined.</li>
    </ul>
  ]]></change-notes>

  <depends>com.intellij.modules.platform</depends>

  <resource-bundle>messages.IdeaTabManagerBundle</resource-bundle>

  <extensions defaultExtensionNs="com.intellij">
    <projectService serviceImplementation="com.whalesea.ideatabmanager.service.TabGroupProjectState"/>
    <notificationGroup id="Tab Groups" displayType="BALLOON"/>
    <toolWindow id="Tab Groups"
                anchor="left"
                icon="/icons/tabGroups.svg"
                factoryClass="com.whalesea.ideatabmanager.toolwindow.TabGroupsToolWindowFactory"/>
  </extensions>

  <actions>
    <group id="IdeaTabManager.TabGroups.EditorPopup"
           popup="true">
      <add-to-group group-id="EditorPopupMenu" anchor="last"/>
      <add-to-group group-id="EditorTabPopupMenu" anchor="last"/>
      <action id="IdeaTabManager.NewGroupWithCurrentTab"
              class="com.whalesea.ideatabmanager.actions.CreateGroupFromCurrentTabAction"/>
      <action id="IdeaTabManager.AddCurrentTabToGroup"
              class="com.whalesea.ideatabmanager.actions.AddCurrentTabToGroupAction"/>
      <action id="IdeaTabManager.RemoveCurrentTabFromGroup"
              class="com.whalesea.ideatabmanager.actions.RemoveCurrentTabFromGroupAction"/>
    </group>
    <group id="IdeaTabManager.TabGroups.ProjectViewPopup"
           class="com.whalesea.ideatabmanager.actions.AddSelectedProjectFilesToGroupActionGroup"
           popup="true">
      <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
    </group>
    <action id="IdeaTabManager.ShowTabGroups"
            class="com.whalesea.ideatabmanager.actions.ShowTabGroupsAction">
      <add-to-group group-id="ToolsMenu" anchor="last"/>
    </action>
  </actions>
</idea-plugin>
